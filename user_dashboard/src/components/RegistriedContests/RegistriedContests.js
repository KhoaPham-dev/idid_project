import React, { Component } from 'react';
import {RenderRegistriedContests} from './RenderRegistriedContests'
export class RegistriedContests extends Component {
    render() {
        return (
           <RenderRegistriedContests/>
        );
    }
}
